import firebase from "firebase/compat/app";
import "firebase/compat/firestore";

const RAW_TEXT = `data here`;
console.log("Imported successfully with text length:", RAW_TEXT.length);
process.exit(0);
